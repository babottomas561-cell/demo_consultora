# Router del dashboard principal. Expone los endpoints:
# GET /dashboard/summary  → métricas clave (KPIs) para la vista principal
# GET /dashboard/charts   → datos agregados para los gráficos del dashboard
