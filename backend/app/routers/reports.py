# Router CRUD para reportes de BI. Expone los endpoints:
# GET    /reports        → listar reportes del usuario autenticado
# POST   /reports        → crear un reporte nuevo
# GET    /reports/{id}   → obtener un reporte específico
# PUT    /reports/{id}   → actualizar un reporte
# DELETE /reports/{id}   → eliminar un reporte
