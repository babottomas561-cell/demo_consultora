# Router de autenticación. Expone los endpoints:
# POST /auth/register  → crear cuenta nueva
# POST /auth/login     → login y devuelve JWT access token
# POST /auth/refresh   → renovar el token expirado
