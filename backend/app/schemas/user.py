# Schemas Pydantic para el recurso User.
# UserCreate: datos para registrar un usuario (email, password).
# UserResponse: datos que devuelve la API (sin password).
# UserLogin: datos para autenticarse (email, password).
