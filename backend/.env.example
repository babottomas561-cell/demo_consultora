# Variables de entorno necesarias para el backend.
# Copiar a .env y completar con los valores reales (nunca commitear .env).
DATABASE_URL=postgresql://user:password@localhost:5432/demo_consultora
SECRET_KEY=cambia_esto_por_una_clave_segura
ENVIRONMENT=development
